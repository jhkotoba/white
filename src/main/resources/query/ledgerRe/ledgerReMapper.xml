<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="ledgerReMapper">

	<!-- SELECT -->

	<!-- 은행 조회 -->
	<select id="selectBankList" resultType="whiteMap" parameterType="whiteMap">
		SELECT
			<if test="mode == 'select'">
			'select' AS state,
			</if>
			bank_seq AS bankSeq,
			bank_name AS bankName,
			bank_account AS bankAccount,
			bank_use_yn AS bankUseYn
		FROM user_bank
		WHERE user_seq = #{userSeq}
		<if test="mode != 'select'">
		AND bank_use_yn = 'Y'
		</if>
		ORDER BY bank_seq ASC
	</select>
	
	<!--  -->
	<select id="selectPurList" resultType="whiteMap" parameterType="whiteMap">
		SELECT
			<if test="mode == 'select'">
			'select' AS state,
			</if>
			pur_seq AS purSeq,
	  		pur_order AS purOrder,
	 		purpose AS purpose
		FROM purpose
		WHERE user_seq = #{userSeq}
		ORDER BY pur_order
	</select>
	
	<!--  -->
	<select id="selectPurDtlList" resultType="whiteMap" parameterType="whiteMap">
		SELECT
			<if test="mode == 'select'">
			'select' AS state,
			</if>
			pur_dtl_seq AS purDtlSeq,
			pur_seq AS purSeq,
			pur_dtl_order AS purDtlOrder,
			pur_detail AS purDetail			
		FROM purpose_detail
		WHERE user_seq = #{userSeq}
		ORDER BY pur_dtl_order	
	</select>

	<!-- 금전기록 리스트 조회  -->
	<select id="selectRecordList" resultType="whiteMap" parameterType="whiteMap">
		SELECT
			<if test="mode == 'select'">
			'select' AS state,
			</if>
			rec.record_seq AS recordSeq,
			DATE_FORMAT(rec.record_date,'%Y-%m-%d %H:%i') AS recordDate,			
			IFNULL(rec.position, '') AS position,
			rec.content,
			rec.pur_seq AS purSeq,
			IFNULL(rec.pur_dtl_seq, '') AS purDtlSeq,
			rec.bank_seq AS bankSeq,
			IFNULL(rec.move_seq, '') AS moveSeq,
			rec.money,
			IFNULL(pur.purpose, 'move')	AS purpose,
			IFNULL(purDtl.pur_detail,'') AS purDetail,
			IFNULL(bank.bank_name, 'cash')	AS bankName,
			IFNULL(bank.bank_account, 'cash')	AS bankAccount,
			IFNULL(bank.bank_use_yn,'X') AS bankUseYn
		FROM money_record_re rec
		LEFT OUTER JOIN purpose pur
			ON rec.pur_seq = pur.pur_seq
		LEFT OUTER JOIN purpose_detail purDtl
			ON rec.pur_dtl_seq = purDtl.pur_dtl_seq
		LEFT OUTER JOIN user_bank bank
    		ON rec.bank_seq = bank.bank_seq
		WHERE rec.user_seq = #{userSeq}		
		AND rec.record_date <![CDATA[ >= ]]> #{startDate}
		AND rec.record_date <![CDATA[ <  ]]> #{endDate}			 	
		ORDER BY rec.record_date ASC,  rec.record_seq ASC				
	</select>
	
	<!-- 금전기록 해당날짜 이전 각각(현금, 은행등등) 금액 합산 조회 -->
	<select id="selectCalPastRecord" resultType="whiteMap" parameterType="java.util.List">
		SELECT
		<foreach collection="list" item="item" separator=",">
			(
			  SELECT 
			    IFNULL(
				    SUM( 
				      CASE
				      WHEN bank_seq = #{item.bankSeq} THEN money
				      WHEN move_seq = #{item.bankSeq} THEN ABS(money)
				      END
				    )
				  ,0)
			  FROM money_record_re
			  WHERE user_seq = #{item.userSeq} 
			  AND record_date <![CDATA[ < ]]>  #{item.startDate}
			)AS ${item.bankName}	
		</foreach>		
	</select>
	
	<!-- 금전기록 해당날짜 이전 금액(통합) 합산 조회 -->
	<select id="selectCalPastAmountRecord" resultType="int" parameterType="whiteMap">		
		SELECT
			IFNULL(SUM(money),0) AS amount
		FROM money_record_re
		WHERE user_seq = #{userSeq}
		AND record_date <![CDATA[ <= ]]> LAST_DAY(#{startDate})
		AND pur_seq <![CDATA[ <> ]]> 0
	</select>
	
	<!-- 목적 사용되는지 확인 -->
	<select id="selectIsUsedPurposeRec" resultType="int" parameterType="java.util.List">
		SELECT COUNT(1) FROM money_record_re
		WHERE pur_seq IN
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.purSeq}
		</foreach>
	</select>
	
	<!-- 목적 삭제시 상세목적에 부모로써 사용되는지 확인 -->
	<select id="selectIsUsedPurposePurDtl" resultType="int" parameterType="java.util.List">
		SELECT COUNT(1) FROM purpose_detail
		WHERE pur_seq IN
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.purSeq}
		</foreach>
	</select>
	
	<!-- 상세목적 사용되는지 확인 -->
	<select id="selectIsUsedPurDtl" resultType="int" parameterType="java.util.List">
		SELECT COUNT(1) FROM money_record_re
		WHERE pur_dtl_seq IN
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.purDtlSeq}
		</foreach>
	</select>
	
	<!-- 은행 사용되는지 확인 -->
	<select id="selectIsUsedBank" resultType="int" parameterType="java.util.List">
		SELECT COUNT(1) FROM money_record_re
		WHERE bank_seq IN
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.bankSeq}
		</foreach>
	</select>
	
	<!-- 가계부 월별 통계 조회(수입, 지출, 누적)  -->
	<select id="selectMonthIEAStats" resultType="whiteMap" parameterType="java.util.List">		
		<foreach collection="list" item="item" separator="UNION ALL" >
		SELECT #{item.ym} AS date,
			IFNULL((
			  SELECT SUM(money) FROM money_record_re
			  WHERE user_seq = #{item.userSeq}
			  AND record_date <![CDATA[ >= ]]> #{item.date}
			  AND record_date <![CDATA[ <= ]]> LAST_DAY(#{item.date})
			  AND pur_seq <![CDATA[ <> ]]> 0
			  AND money <![CDATA[ > ]]> 0
			),0) AS plus,
			IFNULL((
			  SELECT SUM(money) FROM money_record_re
			  WHERE user_seq = #{item.userSeq}
			  AND record_date <![CDATA[ >= ]]> #{item.date}
			  AND record_date <![CDATA[ <= ]]> LAST_DAY(#{item.date})
			  AND pur_seq <![CDATA[ <> ]]> 0
			  AND money <![CDATA[ < ]]> 0  
			),0) AS minus
		</foreach>
	</select>
	
	<!-- 가계부 월별 통계 조회(현금, 은행별)  -->
	<select id="selectMonthCBStats" resultType="whiteMap" parameterType="whiteMap">
	 <foreach collection="dateList" item="date" separator="UNION ALL" >
		SELECT
			#{date.ym} AS date,
			<foreach collection="CBList" item="bank" separator="," >
			IFNULL((
			  SELECT			  	
			  	SUM( 
			      CASE
			      WHEN bank_seq = #{bank.bankSeq} THEN money
			      WHEN move_seq = #{bank.bankSeq} THEN ABS(money)
			      END
			    ) 			    
			  FROM money_record_re
			  WHERE user_seq = #{date.userSeq}
			  AND record_date <![CDATA[ >= ]]> #{date.date}
			  AND record_date <![CDATA[ <= ]]> LAST_DAY(#{date.date})
			),0) AS	<choose>
						<when test="bank.bankSeq == 0">
							cash
						</when>
						<otherwise>
							bank${bank.bankSeq}
						</otherwise>
					</choose>
			</foreach>
		</foreach>
	</select>
	
	<!-- 가계부 월별 통계 조회(목적별)  -->
	<select id="selectMonthPStats" resultType="whiteMap" parameterType="whiteMap">
		<foreach collection="dateList" item="date" separator="UNION ALL" >
		SELECT
			#{date.ym} AS date,
			<foreach collection="purList" item="pur" separator="," >
		  	(
		  		SELECT ABS(SUM(money)) FROM money_record_re
			    WHERE user_seq = #{date.userSeq}
			    AND pur_seq = #{pur.purSeq}
			    AND money <![CDATA[ < ]]> 0
			    AND record_date <![CDATA[ >= ]]> #{date.date}
			    AND record_date <![CDATA[ <= ]]> LAST_DAY(#{date.date})
		  	) AS pur${pur.purSeq}
			</foreach>
		</foreach>
	</select>
	

	
	
	
	
	<!-- INSERT  -->
	
	<!--  -->
	<insert id="insertRecordList" parameterType="java.util.List">
		INSERT INTO money_record_re(		
			user_seq,	
			record_date,
			position,	
			content,	
			pur_seq,
			pur_dtl_seq,
			bank_seq,
			move_seq,
			money,
			reg_date
		)VALUES
		<foreach collection="list" item="item" separator="),(" open="(" close=")">
			#{item.userSeq}, 
			#{item.date},
			#{item.position},
			#{item.content}, 
			#{item.purSeq},	
			<choose>
				<when test="item.purDtlSeq == ''">
					NULL,
				</when>
				<otherwise>				
					#{item.purDtlSeq},
				</otherwise>
			</choose>
			#{item.bankSeq}, 
			<choose>
				<when test="item.moveSeq == ''">
					NULL,
				</when>
				<otherwise>				
					#{item.moveSeq},
				</otherwise>
			</choose>			 
			#{item.money},		
			now()
		</foreach>	
	</insert>
	
	
	<!-- 목적 리스트 insert -->
	<insert id="insertPurList" parameterType="java.util.List">
		INSERT INTO purpose(		
			user_seq,	
			pur_order,
			purpose
		)VALUES
		<foreach collection="list" item="item" separator="),(" open="(" close=")">
			#{item.userSeq}, 
			#{item.purOrder},
			#{item.purpose}			
		</foreach>	
	</insert>
	
	<!-- 상세목적 리스트 insert -->
	<insert id="insertPurDtlList" parameterType="java.util.List">
		INSERT INTO purpose_detail(		
			pur_seq,	
			user_seq,	
			pur_dtl_order,
			pur_detail
		)VALUES
		<foreach collection="list" item="item" separator="),(" open="(" close=")">
			#{item.purSeq}, 
			#{item.userSeq}, 
			#{item.purDtlOrder},
			#{item.purDetail}			
		</foreach>	
	</insert>
	
	<!-- 은행 리스트 insert -->
	<insert id="insertBankList" parameterType="java.util.List">
		INSERT INTO user_bank(		
			user_seq,	
			bank_name,
			bank_account,
			bank_use_yn			
		)VALUES
		<foreach collection="list" item="item" separator="),(" open="(" close=")">
			#{item.userSeq}, 
			#{item.bankName},
			#{item.bankAccount},		
			#{item.bankUseYn}	
		</foreach>	
	</insert>
	
	
	<!-- UPDATE -->
	
	<!-- 금전기록 list 수정 -->
	<update id="updateRecordList" parameterType="java.util.List">		
		UPDATE money_record_re
		SET record_date = 
		CASE		
		<foreach collection="list" item="item">
			WHEN record_seq = #{item.recordSeq} THEN #{item.recordDate}
		</foreach>		
		END,	
		content = 
		CASE		
		<foreach collection="list" item="item">
			WHEN record_seq = #{item.recordSeq} THEN #{item.content}
		</foreach>		
		END,	
		position = 
		CASE		
		<foreach collection="list" item="item">
			WHEN record_seq = #{item.recordSeq} THEN #{item.position}
		</foreach>		
		END,		
		pur_seq = 
		CASE		
		<foreach collection="list" item="item">
			WHEN record_seq = #{item.recordSeq} THEN #{item.purSeq}
		</foreach>		
		END,		
		pur_dtl_seq = 
		CASE		
		<foreach collection="list" item="item">
			<choose>
				<when test="item.purDtlSeq == ''">
					WHEN record_seq = #{item.recordSeq} THEN null
				</when>				
				<otherwise>
					WHEN record_seq = #{item.recordSeq} THEN #{item.purDtlSeq}
				</otherwise>
			</choose>			
		</foreach>		
		END,
		bank_seq = 
		CASE		
		<foreach collection="list" item="item">
			WHEN record_seq = #{item.recordSeq} THEN #{item.bankSeq}					
		</foreach>		
		END,		
		move_seq = 
		CASE		
		<foreach collection="list" item="item">
			<choose>				
				<when test="item.moveSeq == ''">
					WHEN record_seq = #{item.recordSeq} THEN NULL
				</when>				
				<otherwise>
					WHEN record_seq = #{item.recordSeq} THEN #{item.moveSeq}
				</otherwise>
			</choose>			
		</foreach>		
		END,		
		money = 
		CASE		
		<foreach collection="list" item="item">
			WHEN record_seq = #{item.recordSeq} THEN #{item.money}
		</foreach>		
		END		
		WHERE record_seq IN 
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.recordSeq}
		</foreach>
	</update>
	
	<!-- 목적 list 수정 -->
	<update id="updatePurList" parameterType="java.util.List">		
		UPDATE purpose
		SET pur_order = 
		CASE		
		<foreach collection="list" item="item">
			WHEN pur_seq = #{item.purSeq} THEN #{item.purOrder}
		</foreach>		
		END,	
		purpose = 
		CASE		
		<foreach collection="list" item="item">
			WHEN pur_seq = #{item.purSeq} THEN #{item.purpose}
		</foreach>		
		END
		WHERE pur_seq IN 
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.purSeq}
		</foreach>
	</update>
	
	<!-- 상세목적 list 수정 -->
	<update id="updatePurDtlList" parameterType="java.util.List">		
		UPDATE purpose_detail
		SET pur_seq = 
		CASE		
		<foreach collection="list" item="item">
			WHEN pur_dtl_seq = #{item.purDtlSeq} THEN #{item.purSeq}
		</foreach>		
		END,
		pur_dtl_order = 
		CASE		
		<foreach collection="list" item="item">
			WHEN pur_dtl_seq = #{item.purDtlSeq} THEN #{item.purDtlOrder}
		</foreach>		
		END,	
		pur_detail = 
		CASE		
		<foreach collection="list" item="item">
			WHEN pur_dtl_seq = #{item.purDtlSeq} THEN #{item.purDetail}
		</foreach>		
		END
		WHERE pur_dtl_seq IN 
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.purDtlSeq}
		</foreach>
	</update>
	
	
	<!-- 은행 list 수정 -->
	<update id="updateBankList" parameterType="java.util.List">		
		UPDATE user_bank		
		SET bank_name = 
		CASE		
		<foreach collection="list" item="item">
			WHEN bank_seq = #{item.bankSeq} THEN #{item.bankName}
		</foreach>		
		END,	
		bank_account = 
		CASE		
		<foreach collection="list" item="item">
			WHEN bank_seq = #{item.bankSeq} THEN #{item.bankAccount}
		</foreach>		
		END,
		bank_use_yn = 
		CASE		
		<foreach collection="list" item="item">
			WHEN bank_seq = #{item.bankSeq} THEN #{item.bankUseYn}
		</foreach>		
		END
		WHERE bank_seq IN 
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.bankSeq}
		</foreach>
	</update>
	
	
	<!-- DELETE -->
	
	<!-- 금전기록 list 삭제 -->
	<delete id="deleteRecordList" parameterType="java.util.List">
		DELETE FROM money_record_re
		WHERE record_seq IN 
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.recordSeq}
		</foreach>
	</delete>
	
	<!-- 목적 list 삭제 -->
	<delete id="deletePurList" parameterType="java.util.List">
		DELETE FROM purpose
		WHERE pur_seq IN 
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.purSeq}
		</foreach>
	</delete>
	
	<!-- 상세목적 list 삭제 -->
	<delete id="deletePurDtlList" parameterType="java.util.List">
		DELETE FROM purpose_detail
		WHERE pur_dtl_seq IN 
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.purDtlSeq}
		</foreach>
	</delete>
	
	<!-- 은행 list 삭제 -->
	<delete id="deleteBankList" parameterType="java.util.List">
		DELETE FROM user_bank
		WHERE bank_seq IN 
		<foreach collection="list" item="item" separator="," open="(" close=")">
			#{item.bankSeq}
		</foreach>
	</delete>
	
</mapper>
